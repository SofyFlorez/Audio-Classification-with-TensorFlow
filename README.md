# Audio-Classification-with-TensorFlow
This project trains a convolutional neural network to classify audio clips into categories like "dog bark" and "siren." A new dataset was created for this project to train on the classes "dog_bark" and "siren" using the UrbanSound8K dataset.
